%% NOTE: Tests currently need working APNS certs and token, as well as access
%% to APNS. Since this APNS service provider is deprecated in favor of the
%% HTTP/2 apns_erlv3, no further effort will be expended to mitigate this
%% issue.
%%
{sessions, []}.


